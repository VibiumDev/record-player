## Plan: Stacked layout mode — IMPLEMENTED

Layout toggle (`"main"` | `"stacked"`) added to TraceStudio. Toolbar button (▤/▥) and `V` keyboard shortcut toggle between modes. In stacked mode, screenshot renders above inspector in a single column with a horizontal resize divider.
